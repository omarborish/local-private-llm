# Local Private LLM – no env vars required for normal use.
# Ollama is expected at http://127.0.0.1:11434 (default).

# Optional: override in code if you need a different Ollama base URL later.
# OLLAMA_BASE_URL=http://127.0.0.1:11434
